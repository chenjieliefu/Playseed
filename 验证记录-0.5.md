# Playseed 0.5 验证记录

> 历史快照：本文件只证明 0.5 当时验证过的范围。当前能力与测试数量见 [当前能力与边界](docs/04-当前能力与边界.md)。

日期：2026-09-05。以下三个项目是本次验证用方案，已标为示例，未冒充用户亲自确认的创作需求。

| 示例 | 实际检查 | 结果 |
|---|---|---|
| 星芽防线 · 射击示例 | 移动、生成子弹、实际命中扣血、12 架击杀目标、生命归零失败、重开 | 通过 |
| 街角咖啡馆 · 经营示例 | 界面按钮连接、制作消耗库存并耗时、出售收入、升级成本与加速、通过正常交易达到目标、资金耗尽、重开 | 通过 |
| 森林灯塔 · 解谜示例 | 坐标点击、开关联动符合线索、两步解法可达、胜利后停止操作、重开、按钮连接 | 通过 |

三个项目均由实际模型请求生成代码；没有通过修改同一份躲避模板实现。均通过 macOS 隔离运行中的脚本检查、120 帧运行、重开检查，并用 Godot 实际渲染画面进行目视检查。

## 修改与恢复

射击示例提交真实修改：“每次射击改为左右两发并排的子弹，保持原来的3点生命、12架敌机、配色、移动和重开功能。”第 2 版返回的实际代码新增双发，测试确认每次发射两个弹体，移动、命中、胜负与重开仍通过。

随后恢复第 1 版，生成第 3 版；再恢复双发的第 2 版，生成第 4 版。恢复后的脚本与目标版本逐字节相同。当前第 4 版为双发，所有历史版本保留。

## 失败与修复

最初射击请求成功；经营请求达到旧的 240 秒超时，解谜请求遇到模型容量不足，均没有伪造游戏成功状态。换用 gpt-5.6-sol，整段代码请求采用 600 秒上限后完成验证。

经营生成的初稿出现变量重复声明，检查拦截后由模型自动修复 1 次，再通过检查。修复过程和日志在 jobs 目录保留。

## 平台检查

- Python 自动检查共 19 项通过，覆盖旧模板、方案、制作清单、版本冲突、取消、失败不覆盖、修改与恢复。
- 实际 sandbox-exec 检查：不能读取测试工程外的样本文件，不能写入工程外路径，不能启动 shell/额外程序，不能连接本机测试网络服务。
- 创作界面检查：首页、项目列表、确认制作、试玩和修改请求带正确版本号、忙碌时禁止换项目。
- 方向键回归：试玩焦点、WASD、空格、输入框编辑、菜单、弹窗、窗口焦点及 Tab 通过。
- 实际工作台画面核对：试玩、修改和版本恢复入口可见，没有超出窗口。

## 验证边界

这些结果验证当前三个小型 2D 示例和平台流程，不代表任意复杂游戏均能成功，也不能替代长期试玩和美术验收。当前使用程序绘制画面，未接外部素材、音效、3D、联网和云端多用户。

可重跑的平台命令见 README。真实游戏专项脚本位于 .playseed/qa/；原始请求、结果、检查与渲染记录位于 .playseed/jobs/。scripts/validate_creation.py 会发起真实模型请求并消耗账户额度。
